# 🗄️ วิธีตั้งค่า Database สำหรับระบบ Login

คู่มือนี้จะช่วยให้คุณสร้าง database ใน Supabase สำหรับระบบ Login/Register เท่านั้น

---

## 📋 ขั้นตอนการตั้งค่า

### 1. สร้าง Supabase Project (ถ้ายังไม่มี)

1. ไปที่ https://supabase.com
2. สร้าง project ใหม่ (ถ้ายังไม่มี)
3. รอให้ project สร้างเสร็จ (ประมาณ 1-2 นาที)

---

### 2. ดึง API Keys

1. ไปที่ **Settings** → **API**
2. คัดลอก:
   - **Project URL** (เช่น `https://xxxxx.supabase.co`)
   - **anon public key** (จากส่วน Project API keys)

---

### 3. ตั้งค่าไฟล์ .env

เปิดไฟล์ `Backend/.env` และเพิ่ม:

```env
PORT=3000
GEMINI_API_KEY=your_gemini_api_key
JWT_SECRET=your_jwt_secret_key_here

# Supabase Configuration
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**ตัวอย่าง JWT_SECRET:**
```env
JWT_SECRET=my_super_secret_jwt_key_12345
```

---

### 4. สร้าง Tables ใน Supabase

1. ไปที่ **SQL Editor** ใน Supabase Dashboard
2. คลิก **"New query"**
3. เปิดไฟล์ `Backend/schema_login_only.sql` ในโปรเจกต์ของคุณ
4. **คัดลอกโค้ดทั้งหมด** จากไฟล์
5. **วางใน SQL Editor** ของ Supabase
6. คลิก **"Run"** หรือกด `Ctrl+Enter` (Windows) / `Cmd+Enter` (Mac)
7. ตรวจสอบว่าเห็นข้อความ **"Success"** หรือ **"Success. No rows returned"**

---

### 5. ตรวจสอบว่า Tables ถูกสร้างแล้ว

1. ไปที่ **Table Editor** ในเมนูด้านซ้าย
2. ควรเห็นตาราง:
   - ✅ **User** - สำหรับเก็บข้อมูลผู้ใช้
   - ✅ **Admin** - สำหรับเก็บข้อมูล admin

---

### 6. ทดสอบระบบ Login

1. รีสตาร์ท Backend:
   ```bash
   cd Backend
   npm run dev
   ```

2. ทดสอบ Register (POST):
   ```bash
   POST http://localhost:3000/api/register
   Content-Type: application/json
   
   {
     "user_email": "test@example.com",
     "user_fname": "Test",
     "user_lname": "User",
     "user_password": "password123",
     "user_tel": "0812345678"
   }
   ```

3. ทดสอบ Login (POST):
   ```bash
   POST http://localhost:3000/api/login
   Content-Type: application/json
   
   {
     "user_email": "test@example.com",
     "user_password": "password123"
   }
   ```

4. ตรวจสอบใน Supabase Dashboard:
   - ไปที่ **Table Editor** → **User**
   - ควรเห็นข้อมูลผู้ใช้ที่สมัครแล้ว

---

## ✅ สิ่งที่ถูกสร้าง

### ตาราง User
- `user_id` - ID ของผู้ใช้ (Primary Key)
- `user_email` - อีเมล (Unique)
- `user_fname` - ชื่อ
- `user_lname` - นามสกุล
- `user_password` - รหัสผ่าน (hashed)
- `user_tel` - เบอร์โทร
- `calorie_limit` - จำกัดแคลอรี่
- `allergies` - ข้อมูลแพ้อาหาร
- `reset_password_token` - Token สำหรับรีเซ็ตรหัสผ่าน
- `reset_password_expires` - วันหมดอายุของ Token
- `created_at` - วันที่สร้าง
- `updated_at` - วันที่อัปเดตล่าสุด

### ตาราง Admin
- `admin_id` - ID ของ Admin (Reference ไปที่ User)
- `created_at` - วันที่สร้าง

---

## 🔧 Features ที่พร้อมใช้งาน

✅ **Register** - `/api/register`  
✅ **Login** - `/api/login`  
✅ **Get Profile** - `/api/me` (ต้อง Login)  
✅ **Update Profile** - `/api/users/profile` (ต้อง Login)  
✅ **Change Password** - `/api/users/change-password` (ต้อง Login)  
✅ **Forgot Password** - `/api/forgot-password`  
✅ **Reset Password** - `/api/reset-password/:token`  
✅ **Preferences** - `/api/preferences` (calorie limit, allergies)  

---

## 🆘 Troubleshooting

### ปัญหา: "Supabase credentials not found"
**แก้ไข:** ตรวจสอบว่าเพิ่ม `SUPABASE_URL` และ `SUPABASE_ANON_KEY` ในไฟล์ `.env` แล้ว

### ปัญหา: "relation does not exist"
**แก้ไข:** ตรวจสอบว่า run `schema_login_only.sql` ใน Supabase SQL Editor แล้ว

### ปัญหา: "permission denied"
**แก้ไข:** ตรวจสอบว่า RLS policies ถูกสร้างแล้ว (ดูใน SQL script)

### ปัญหา: "JWT_SECRET is not defined"
**แก้ไข:** เพิ่ม `JWT_SECRET` ในไฟล์ `.env`

---

## 📝 หมายเหตุ

- **ไฟล์ `.env`** ควรอยู่ใน `.gitignore`
- **ห้าม commit** API keys หรือ secrets ไปที่ Git
- Database นี้ใช้สำหรับ **ระบบ Login เท่านั้น**
- ถ้าต้องการเพิ่ม features อื่นๆ (Menu, Posts, etc.) ให้ run `schema.sql` แทน

---

## 🎉 เสร็จสิ้น!

ตอนนี้ระบบ Login พร้อมใช้งานแล้ว! คุณสามารถ:
- สมัครสมาชิกใหม่
- Login เข้าระบบ
- จัดการ Profile
- ใช้ระบบ Password Reset

ข้อมูลจะถูกเก็บไว้ใน Supabase Cloud Database และพร้อมใช้งานจากทุกเครื่องที่เชื่อมต่ออินเทอร์เน็ต!

