# 🗄️ วิธีตั้งค่า Database สำหรับระบบ Community

คู่มือนี้จะช่วยให้คุณสร้าง database ใน Supabase สำหรับระบบ Community (โพสต์, คอมเมนต์, ไลค์)

---

## 📋 ขั้นตอนการตั้งค่า

### 1. ตรวจสอบว่า Supabase Project พร้อมแล้ว

- ถ้ายังไม่มี Supabase project → ดู [README_CLOUD_DATABASE.md](./README_CLOUD_DATABASE.md)
- ถ้ามีแล้ว → ไปขั้นตอนต่อไป

---

### 2. สร้าง Tables ใน Supabase

1. ไปที่ **SQL Editor** ใน Supabase Dashboard
2. คลิก **"New query"**
3. เปิดไฟล์ `Backend/schema_community.sql` ในโปรเจกต์ของคุณ
4. **คัดลอกโค้ดทั้งหมด** จากไฟล์
5. **วางใน SQL Editor** ของ Supabase
6. คลิก **"Run"** หรือกด `Ctrl+Enter` (Windows) / `Cmd+Enter` (Mac)
7. ตรวจสอบว่าเห็นข้อความ **"Success"** หรือ **"Success. No rows returned"**

---

### 3. ตรวจสอบว่า Tables ถูกสร้างแล้ว

1. ไปที่ **Table Editor** ในเมนูด้านซ้าย
2. ควรเห็นตาราง:
   - ✅ **CommunityPost** - สำหรับเก็บโพสต์
   - ✅ **CommunityComment** - สำหรับเก็บความคิดเห็น
   - ✅ **PostLike** - สำหรับเก็บไลค์
   - ✅ **CommunityReport** - สำหรับรายงานโพสต์

---

### 4. ตรวจสอบว่า User Table มีอยู่แล้ว

ระบบ Community ต้องใช้ตาราง **User** ด้วย ถ้ายังไม่มี:

1. Run `Backend/schema_login_only.sql` ก่อน (ดู [SETUP_LOGIN_DATABASE.md](./SETUP_LOGIN_DATABASE.md))

---

## ✅ สิ่งที่ถูกสร้าง

### ตาราง CommunityPost
- `cpost_id` - ID ของโพสต์ (Primary Key)
- `cpost_title` - หัวข้อโพสต์
- `cpost_content` - เนื้อหาโพสต์ (optional)
- `cpost_datetime` - วันที่สร้างโพสต์
- `cpost_image` - ไฟล์รูปภาพ (optional)
- `like_count` - จำนวนไลค์
- `user_id` - ID ของผู้ใช้ที่สร้างโพสต์ (Reference ไปที่ User)

### ตาราง CommunityComment
- `comment_id` - ID ของคอมเมนต์ (Primary Key)
- `comment_text` - ข้อความคอมเมนต์
- `comment_datetime` - วันที่สร้างคอมเมนต์
- `cpost_id` - ID ของโพสต์ (Reference)
- `user_id` - ID ของผู้ใช้ที่สร้างคอมเมนต์ (Reference)

### ตาราง PostLike
- `id` - ID ของไลค์ (Primary Key, Auto-increment)
- `post_id` - ID ของโพสต์ (Reference)
- `user_id` - ID ของผู้ใช้ที่ไลค์ (Reference)
- `unique (post_id, user_id)` - ป้องกันไม่ให้ไลค์ซ้ำ

### ตาราง CommunityReport
- `creport_id` - ID ของรายงาน (Primary Key)
- `creport_reason` - เหตุผลในการรายงาน
- `creport_datetime` - วันที่รายงาน
- `cpost_id` - ID ของโพสต์ที่ถูกรายงาน (Reference)
- `user_id` - ID ของผู้ใช้ที่รายงาน (Reference, nullable)

---

## 🔧 Features ที่พร้อมใช้งาน

✅ **สร้างโพสต์** - `/api/posts` (POST)  
✅ **ดูโพสต์ทั้งหมด** - `/api/posts` (GET)  
✅ **ดูโพสต์เดียว** - `/api/posts/:id` (GET)  
✅ **เพิ่มความคิดเห็น** - `/api/posts/:id/comments` (POST)  
✅ **ไลค์/ยกเลิกไลค์** - `/api/posts/:id/like` (POST)  
✅ **ลบโพสต์** - `/api/posts/:id` (DELETE)  

---

## 🆘 Troubleshooting

### ปัญหา: "relation does not exist"
**แก้ไข:** ตรวจสอบว่า run `schema_community.sql` ใน Supabase SQL Editor แล้ว

### ปัญหา: "foreign key constraint"
**แก้ไข:** ตรวจสอบว่ามีตาราง **User** อยู่แล้ว (run `schema_login_only.sql` ก่อน)

### ปัญหา: "permission denied"
**แก้ไข:** ตรวจสอบว่า RLS policies ถูกสร้างแล้ว (ดูใน SQL script)

---

## 📝 หมายเหตุ

- **Database นี้ใช้สำหรับระบบ Community เท่านั้น**
- **ต้องมี User table ก่อน** ถึงจะสร้าง Community tables ได้
- **RLS policies** ถูกตั้งค่าให้ Backend สามารถเข้าถึงได้ทั้งหมด

---

## 🎉 เสร็จสิ้น!

ตอนนี้ระบบ Community พร้อมใช้งานแล้ว! คุณสามารถ:
- สร้างโพสต์ใหม่
- ดูโพสต์ทั้งหมด
- เพิ่มความคิดเห็น
- ไลค์โพสต์
- ลบโพสต์ (ถ้าเป็นเจ้าของหรือ Admin)

ข้อมูลจะถูกเก็บไว้ใน Supabase Cloud Database และพร้อมใช้งานจากทุกเครื่องที่เชื่อมต่ออินเทอร์เน็ต!

