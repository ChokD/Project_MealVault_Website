# 📋 วิธีการตั้งค่า Chatbot

## วิธีแก้ปัญหา "เกิดข้อผิดพลาดในการสื่อสารกับ AI"

### ขั้นตอนที่ 1: สร้างไฟล์ `.env`
ในโฟลเดอร์ `Backend` สร้างไฟล์ชื่อ `.env` แล้วใส่:

```env
PORT=3000
GEMINI_API_KEY=paste_your_api_key_here
```

### ขั้นตอนที่ 2: ดาวน์โหลด Gemini API Key
1. ไปที่: https://makersuite.google.com/app/apikey
2. คลิก **"Create API Key"**
3. คัดลอก Key
4. วางในไฟล์ `.env` หลัง `GEMINI_API_KEY=`

### ขั้นตอนที่ 3: รีสตาร์ท Backend
หยุด Backend server (Ctrl+C) แล้วรันใหม่:

```bash
cd Backend
npm run dev
```

### ขั้นตอนที่ 4: ตรวจสอบ
ดูที่ Terminal ควรเห็น:
```
Server is running on port 3000
```

### ⚠️ หากยังมีปัญหา
ให้ดู error message ใน Terminal ของ Backend เพื่อหาสาเหตุที่แท้จริง

---
**หมายเหตุ:** 
- ไฟล์ `.env` จะไม่ถูก commit ไปที่ Git (ปกติ)
- API Key เป็นความลับ ห้ามแชร์กับใคร

