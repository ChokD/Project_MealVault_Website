# ☁️ วิธีตั้งค่า Cloud Database (Supabase)

คู่มือนี้จะช่วยให้คุณตั้งค่า **Supabase Cloud Database** เพื่อให้ข้อมูลของคุณพร้อมใช้งานจากทุกเครื่องที่เชื่อมต่ออินเทอร์เน็ตได้

---

## 🎯 ทำไมต้องใช้ Cloud Database?

✅ **ข้อมูลพร้อมใช้งานทุกที่** - เปิดเครื่องไหนก็เข้าถึงข้อมูลเดียวกัน  
✅ **ไม่ต้องติดตั้ง MySQL บนเครื่อง** - ใช้ผ่าน Cloud  
✅ **Backup อัตโนมัติ** - ข้อมูลปลอดภัย  
✅ **ฟรี** - Supabase มี Free Tier ที่เพียงพอสำหรับการพัฒนา  

---

## 📋 ขั้นตอนการตั้งค่า

### 1. สร้าง Supabase Project

1. ไปที่เว็บไซต์: https://supabase.com
2. คลิก **"Start your project"** หรือ **"Sign In"** (ถ้ามีบัญชีอยู่แล้ว)
3. คลิก **"New Project"**
4. กรอกข้อมูล:
   - **Name**: `mealvault` (หรือชื่อที่คุณต้องการ)
   - **Database Password**: ตั้งรหัสผ่านที่แข็งแรง (บันทึกไว้!)
   - **Region**: เลือก region ที่ใกล้ที่สุด (เช่น `Southeast Asia (Singapore)`)
5. คลิก **"Create new project"**
6. รอสักครู่ให้ Supabase สร้าง project ให้ (ประมาณ 1-2 นาที)

---

### 2. ดึง API Keys

1. เมื่อ project สร้างเสร็จแล้ว ให้ไปที่ **Settings** (ไอคอนฟันเฟือง)
2. คลิก **"API"** ในเมนูด้านซ้าย
3. คัดลอกข้อมูลต่อไปนี้:

   - **Project URL**: อยู่ที่ด้านบน (เช่น `https://xxxxx.supabase.co`)
   - **anon public key**: อยู่ที่ส่วน **Project API keys** (คัดลอก `anon` `public` key)

---

### 3. ตั้งค่าไฟล์ .env

เปิดไฟล์ `.env` ในโฟลเดอร์ `Backend` และเพิ่มข้อมูล Supabase:

```env
# Supabase Configuration
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**ตัวอย่าง:**
```env
PORT=3000
GEMINI_API_KEY=your_gemini_api_key
JWT_SECRET=your_jwt_secret_key

# Supabase Configuration
SUPABASE_URL=https://abcdefghijklmnop.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFiY2RlZmdoaWprbG1ub3AiLCJyb2xlIjoiYW5vbiIsImlhdCI6MTYxNjIzOTAyMiwiZXhwIjoxOTMxODE1MDIyfQ.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

---

### 4. สร้าง Tables ใน Supabase

1. ไปที่ **SQL Editor** ในเมนูด้านซ้ายของ Supabase Dashboard
2. คลิก **"New query"**
3. เปิดไฟล์ `Backend/schema.sql` ในโปรเจกต์ของคุณ
4. คัดลอกโค้ดทั้งหมดจากไฟล์ `schema.sql`
5. วางใน SQL Editor ของ Supabase
6. คลิก **"Run"** หรือกด `Ctrl+Enter`
7. ตรวจสอบว่าไม่มี error และเห็นข้อความ **"Success"**

---

### 5. ตรวจสอบการเชื่อมต่อ

1. รีสตาร์ท Backend server:
   ```bash
   cd Backend
   npm run dev
   ```

2. ตรวจสอบ Terminal ว่าไม่มี error

3. ทดสอบ API:
   - เปิดเบราว์เซอร์ไปที่ `http://localhost:3000/api/posts`
   - ควรเห็นข้อมูล (อาจเป็น array ว่างถ้ายังไม่มีข้อมูล)

---

## ✅ ตรวจสอบว่าใช้งานได้

### วิธีที่ 1: ดูใน Supabase Dashboard
1. ไปที่ **Table Editor** ในเมนูด้านซ้าย
2. ควรเห็น tables ทั้งหมดที่สร้างไว้ (User, Menu, Category, CommunityPost, etc.)

### วิธีที่ 2: ทดสอบผ่าน API
เปิดเบราว์เซอร์ไปที่:
- `http://localhost:3000/api/posts` - ดูโพสต์
- `http://localhost:3000/api/categories` - ดูหมวดหมู่

---

## 🔄 ย้ายข้อมูลจาก MySQL ไป Supabase

ถ้าคุณมีข้อมูลใน MySQL อยู่แล้วและต้องการย้ายไป Supabase:

### วิธีที่ 1: Export/Import ผ่าน SQL
1. Export ข้อมูลจาก MySQL:
   ```bash
   mysqldump -u root -p mealvault_db > backup.sql
   ```

2. แปลง SQL ให้เข้ากับ PostgreSQL syntax (Supabase ใช้ PostgreSQL)

3. Import ไป Supabase ผ่าน SQL Editor

### วิธีที่ 2: ใช้ Supabase Dashboard
- ใช้ **Table Editor** เพื่อเพิ่มข้อมูลทีละ record
- หรือใช้ **Import** feature ใน Supabase Dashboard

---

## 🎯 ใช้งานจากเครื่องอื่น

เมื่อตั้งค่าเสร็จแล้ว คุณสามารถ:

1. **Clone โปรเจกต์** บนเครื่องอื่น
2. **ตั้งค่าไฟล์ .env** ด้วย Supabase credentials เดียวกัน
3. **รัน Backend** - ข้อมูลจะมาจาก Supabase Cloud Database

**ไม่ต้อง:**
- ❌ ติดตั้ง MySQL บนเครื่องใหม่
- ❌ Import ข้อมูลซ้ำ
- ❌ กังวลเรื่อง backup

---

## 🔐 Security Notes

⚠️ **สำคัญ:** 
- ไฟล์ `.env` ควรอยู่ใน `.gitignore` 
- **ห้าม commit** API keys ไปที่ Git
- ใช้ `anon` key สำหรับ Backend (ไม่ใช่ `service_role` key)

---

## 🆘 Troubleshooting

### ปัญหา: "Supabase credentials not found"
**แก้ไข:** ตรวจสอบว่าเพิ่ม `SUPABASE_URL` และ `SUPABASE_ANON_KEY` ในไฟล์ `.env` แล้ว

### ปัญหา: "relation does not exist"
**แก้ไข:** ตรวจสอบว่า run `schema.sql` ใน Supabase SQL Editor แล้ว

### ปัญหา: "permission denied"
**แก้ไข:** ตรวจสอบว่า RLS policies ถูกตั้งค่าถูกต้อง (ดูใน `schema.sql`)

---

## 📚 ข้อมูลเพิ่มเติม

- [Supabase Documentation](https://supabase.com/docs)
- [Supabase Dashboard](https://app.supabase.com)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)

---

## 🎉 เสร็จสิ้น!

เมื่อตั้งค่าเสร็จแล้ว ข้อมูลของคุณจะถูกเก็บไว้ใน Cloud และพร้อมใช้งานจากทุกเครื่องที่เชื่อมต่ออินเทอร์เน็ตได้แล้ว!

