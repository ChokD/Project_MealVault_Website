# 🚀 วิธีรันเว็บไซต์ MealVault

## 📋 ข้อกำหนดเบื้องต้น

ก่อนเริ่มต้น ต้องติดตั้ง:
- **Node.js** (เวอร์ชัน 18 ขึ้นไป)
- **npm** หรือ **yarn**

**สำหรับฐานข้อมูล:**
- **Option 1 (แนะนำ):** ใช้ **Supabase Cloud Database** (ไม่ต้องติดตั้ง MySQL) - ดูวิธีตั้งค่าใน [README_CLOUD_DATABASE.md](./README_CLOUD_DATABASE.md)
- **Option 2:** ติดตั้ง **MySQL** บนเครื่องของคุณเอง

---

## 🔧 ขั้นตอนการตั้งค่า

### 1. ติดตั้ง Dependencies

#### Backend
```bash
cd Backend
npm install
```

#### Frontend
```bash
cd Frontend/frontend
npm install
```

---

### 2. ตั้งค่าไฟล์ .env สำหรับ Backend

สร้างไฟล์ `.env` ในโฟลเดอร์ `Backend` และใส่ข้อมูลดังนี้:

```env
# Server Port
PORT=3000

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=mealvault_db

# Gemini AI API Key (สำหรับ Chatbot)
GEMINI_API_KEY=your_gemini_api_key

# JWT Secret (สำหรับการ Authentication)
JWT_SECRET=your_jwt_secret_key

# Email Configuration (สำหรับการส่งอีเมล)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_email_password

# Supabase Configuration (สำหรับ Cloud Database - แนะนำ)
# ดูวิธีตั้งค่าใน README_CLOUD_DATABASE.md
SUPABASE_URL=your_supabase_url
SUPABASE_ANON_KEY=your_supabase_anon_key
```

**วิธีสร้าง Gemini API Key:**
1. ไปที่ https://makersuite.google.com/app/apikey
2. คลิก "Create API Key"
3. คัดลอก Key และวางในไฟล์ `.env`

---

### 3. ตั้งค่าฐานข้อมูล

**เลือกวิธีใดวิธีหนึ่ง:**

#### Option 1: ใช้ Supabase Cloud Database (แนะนำ) ⭐

**ข้อดี:** ข้อมูลพร้อมใช้งานทุกเครื่อง, ไม่ต้องติดตั้ง MySQL, Backup อัตโนมัติ

1. ดูคำแนะนำเต็มใน [README_CLOUD_DATABASE.md](./README_CLOUD_DATABASE.md)
2. สร้าง Supabase project ที่ https://supabase.com
3. เพิ่ม `SUPABASE_URL` และ `SUPABASE_ANON_KEY` ในไฟล์ `.env`
4. Run `Backend/schema.sql` ใน Supabase SQL Editor

#### Option 2: ใช้ MySQL บนเครื่อง

1. สร้างฐานข้อมูล:
```sql
CREATE DATABASE mealvault_db;
```

2. Import schema จากไฟล์ `MySQL/mealvault_db.sql`:
```bash
mysql -u root -p mealvault_db < MySQL/mealvault_db.sql
```

---

## 🎯 วิธีรันเว็บไซต์

### วิธีที่ 1: รันแยกกัน (แนะนำสำหรับการพัฒนา)

#### Terminal 1: รัน Backend
```bash
cd Backend
npm run dev
```

Backend จะรันที่ `http://localhost:3000`

#### Terminal 2: รัน Frontend
```bash
cd Frontend/frontend
npm run dev
```

Frontend จะรันที่ `http://localhost:5173` (หรือ port อื่นที่ Vite แสดง)

---

### วิธีที่ 2: รันทั้งสองพร้อมกัน (ใช้ PowerShell หรือ Terminal แยก)

เปิด **2 Terminal Windows**:

**Terminal 1 (Backend):**
```powershell
cd Backend
npm run dev
```

**Terminal 2 (Frontend):**
```powershell
cd Frontend\frontend
npm run dev
```

---

## ✅ ตรวจสอบการทำงาน

### Backend
- เปิดเบราว์เซอร์ไปที่ `http://localhost:3000`
- ควรเห็นข้อความ: `Hello, MealVault Backend!`
- ตรวจสอบ Terminal ว่าเห็น: `Server is running on port 3000`

### Frontend
- เปิดเบราว์เซอร์ไปที่ `http://localhost:5173` (หรือ port ที่ Vite แสดง)
- ควรเห็นหน้าเว็บไซต์ MealVault

---

## 🔍 Troubleshooting

### ปัญหา: Database connection error
**แก้ไข:** ตรวจสอบว่า MySQL ทำงานอยู่ และข้อมูลในไฟล์ `.env` ถูกต้อง

### ปัญหา: Port 3000 ถูกใช้งานแล้ว
**แก้ไข:** เปลี่ยน PORT ในไฟล์ `.env` เป็น port อื่น (เช่น 3001)

### ปัญหา: Frontend ไม่เชื่อมต่อกับ Backend
**แก้ไข:** ตรวจสอบว่า Backend กำลังรันอยู่ และ CORS ถูกตั้งค่าถูกต้อง

### ปัญหา: Chatbot ไม่ทำงาน
**แก้ไข:** ตรวจสอบว่า GEMINI_API_KEY ในไฟล์ `.env` ถูกต้อง

---

## 📝 หมายเหตุ

- **ไฟล์ `.env`** ควรอยู่ใน `.gitignore` เพื่อไม่ให้ commit ไปที่ Git
- **API Keys** เป็นความลับ ห้ามแชร์กับใคร
- ระหว่างการพัฒนา Backend จะใช้ `nodemon` เพื่อ restart อัตโนมัติเมื่อมีการแก้ไขไฟล์

---

## 🎉 พร้อมใช้งาน!

เมื่อรันทั้ง Backend และ Frontend แล้ว คุณสามารถใช้งานเว็บไซต์ MealVault ได้แล้ว!

